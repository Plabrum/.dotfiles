return {
  "saghen/blink.cmp",
  opts = {
    keymap = {
      preset = "enter",
      -- ["<Tab>"] = {
      --   require("blink.cmp.keymap.presets").get("super-tab")["<Tab>"][1],
      --   LazyVim.cmp.map({ "snippet_forward", "ai_accept" }),
      --   "fallback",
      -- },
    },
  },
}
